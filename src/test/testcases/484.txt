Function: main
        Parameter: n
        Body:
            Var: t1 = 0, t2 = 1, nextTerm = 0;
            print("Enter the number of terms: ");
            getline(n);
            print("Fibonacci Series: ");
            For (i = 1, i <= n, 1) Do
                If(i == 1) Then
                printStr(" " + t1);
                Continue;
                EndIf.
            If(i == 2) Then
                printStr( t2+" ");
        Continue;
        EndIf.
        nextTerm = t1 + t2;
        t1 = t2;
        t2 = nextTerm;
        
        printStr(nextTerm + " ");
    EndFor.
    Return 0;
    EndBody.