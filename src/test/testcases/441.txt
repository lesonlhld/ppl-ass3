
        Var: x, a,b ,c;
        Function: main 
        Body:
        Var:a,b,c;
            If (x == (b!=c && (a > b + c))) Then Return;
            ElseIf (x=="Chung Xon@@") Then Break;
            Else 
            x="successful";
            EndIf.
        EndBody.